{
  "schema": 2,
  "build": {
    "builder": "DOCKERFILE",
    "watchPatterns": [
      "/app/src/**",
      "/app/package.json"
    ]
  },
  "deploy": {
    "startCommand": "npm start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE"
  }
}